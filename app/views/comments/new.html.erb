<h1> Add a Comment </h1>


<%= form_for(@comment) do |f| %>

  <%= f.label :content %>
  <%= f.text_area :content %>
  <br>
  <br>
  <%= f.hidden_field :job_id %>
  <br>
  <br>
  <%= f.submit "Add a Comment" %>

<% end %>
