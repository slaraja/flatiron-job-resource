<!DOCTYPE html>
<html>
  <head>
    <title>FlatironJobResource</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>


  <body>
    <%= yield %>
  </body>

  <div> 
    <br>
    <br>
    <h2> Menu <h2> 
    <%= link_to "See Job List", jobs_path if session[:user_id].present? %>
    <br>
    <br>
    <%= link_to "Add a Job", new_job_path if session[:user_id].present? %>
    <br>
    <br> 
    <%= link_to "See Company List", companies_path if session[:user_id].present? %>
    <br>
    <br>
    <%= link_to "Log Out", logout_path, method: :delete if session[:user_id].present? %>
  <div>

</html>
